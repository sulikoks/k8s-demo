name: Docker Image CI

on:
  push:
    branches: [ "master" ]

jobs:

  tests:
    runs-on: ubuntu-latest
    environment: prod
    env:
      DOCKERHUB: 123
    steps:
      - name: Tests1
        run: echo "here must be tests $GITHUB_JOB"
#      - name: Tests2
#        run: echo "$TEST + $DOCKERHUB_HOST"
      - name: Tests3
        run: echo "$DOCKERHUB"

#  build:
#    runs-on: ubuntu-latest
#    steps:
#    - uses: actions/checkout@v3
#    - name: Build the Docker image
#      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
#    - name: Push the Docker image
#      run:
